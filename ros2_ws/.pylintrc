[MASTER]
# ROS2 패키지들(rclpy, cv_bridge, nav2_msgs, sensor_msgs, pinky_lcd 등)은
# 로봇 런타임에만 설치되어 있어, 개발 머신에서는 import-error가 정상적으로 발생함.
# 로컬 lint에서는 무시하고 실제 로봇 환경에서 실행 확인.

[MESSAGES CONTROL]
disable=import-error
